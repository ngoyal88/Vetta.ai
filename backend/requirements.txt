# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart

# Data Models & Validation
# Align with pipecat-ai which requires pydantic ~= 2.8.2
pydantic==2.8.2
pydantic-settings==2.2.0

# HTTP Client
httpx==0.26.0
requests

# Database & Caching
redis==5.0.2
firebase_admin

# Resume Parsing
PyPDF2==3.0.1
python-docx==0.8.11

# AI/ML Services
google-generativeai      # Gemini LLM
numpy>=1.24              # Required by faster-whisper

# LiveKit + Pipecat Stack
livekit==0.15.1
livekit-agents==0.5.1
pipecat-ai==0.0.45

# Speech Services (Choose one STT + one TTS)
# STT Options:
deepgram-sdk==3.2.7      # RECOMMENDED (best quality/speed)
# openai>=1.0.0          # Alternative STT

# TTS Options:
elevenlabs==1.2.2        # RECOMMENDED (most natural)
# cartesia-ai>=1.0.0     # Alternative (fast)
# openai>=1.0.0          # Alternative TTS                # Free Text-to-Speech

# Utilities
python-dotenv==1.0.1
aiohttp