export const feedbackKey = (sessionId: string) => `interview_feedback_${sessionId}`;
export const codeBackupKey = (sessionId: string) => `roundr_code_backup_${sessionId}`;
export const codeLangKey = (sessionId: string) => `roundr_code_lang_${sessionId}`;
