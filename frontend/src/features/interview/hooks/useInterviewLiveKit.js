/**
 * React hook for a LiveKit-based interview session.
 *
 * Exported for backward compatibility with existing consumers.
 */
export { useInterviewLiveKitAdapter as useInterviewLiveKit } from './useInterviewLiveKitAdapter';
